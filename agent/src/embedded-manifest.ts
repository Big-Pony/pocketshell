// Stub — overwritten by scripts/gen-embedded.ts during `bun run build:bin`,
// then restored after compile. In dev/test no assets are embedded (the vite
// dev server serves the frontend on :5173). Do not put generated content here.
export const ASSETS: Record<string, string> = {};
