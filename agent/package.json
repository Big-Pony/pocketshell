{
  "name": "pocketshell-agent",
  "version": "0.1.0",
  "description": "PocketShell Agent — Bun backend that serves the embedded frontend and manages tmux-backed PTY sessions over an encrypted WebSocket.",
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/Big-Pony/pocketshell.git" },
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun test",
    "gen:embedded": "bun run scripts/gen-embedded.ts",
    "build:bin": "bun run scripts/build-bin.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  },
  "dependencies": {
    "noise-handshake": "^4.2.0",
    "sodium-javascript": "^0.8.0"
  },
  "overrides": {
    "sodium-native": "npm:sodium-javascript@^0.8.0"
  }
}
