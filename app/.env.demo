# 演示构建（bun run build:demo，即 `vite build --mode demo`）专用环境文件。
# Vite 按 mode 自动加载 .env.<mode>，把下面的 VITE_ 前缀变量注入
# import.meta.env——App.svelte / SettingsPanel.svelte 的演示门控
# `import.meta.env.VITE_POCKETSHELL_DEMO === "1"` 靠它在构建期变真。
# 不是密钥，故意提交进版本控制（不在 .gitignore 的 .env / .env.local 之列）。
VITE_POCKETSHELL_DEMO=1
